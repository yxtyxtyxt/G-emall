1、banner图点击后会额外停滞3秒（暂未解决）。
解决方法：未解决。

2、引入jq1.8.3和jq.cookie后，$.cookie无法使用。
解决方法：在检查无引入顺序问题后，使用原生JS手动封装设置、调取和删除cookie的函数。

3、主页banner图右下角滚动公告无法做到无缝轮播。
解决方法：更改多次代码后无果，使用插件解决。

4、主页banner图右侧小广告图css样式覆盖。
解决方法：增加其权重，解决。

5、楼层模块复制后，JS动画效果混乱，具体表现为楼层中banner图自动轮播顺序混乱，且不同楼层无法同时播放；左侧logo左右按钮失效。
解决方法：更改JS中的函数，将动画效果封装成函数，在主页调用时填入每层楼不同的ID即可。

6、详情页中数量框可输入英文字母
解决方法：设置keyup事件，使用正则判断文本框中内容，若不为数字，则强制变为1

7、加入购物车时飞入效果未实现，插件无法使用。
解决方法：未解决。